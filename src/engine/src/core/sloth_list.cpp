#include "core/sloth_list.h"

// FixedList is a header-only template; there is no non-template code to
// compile here. This translation unit exists so premake picks up sloth_list
// as a normal engine source pair (see core/sloth_list.h for the API).
